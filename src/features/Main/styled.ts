import styled from 'styled-components';

const MainStyled = styled.div`
    .mainWrap {
        height: 10000px;
        text-align: center;
        padding-top: 50px;
    }
`;

export default MainStyled;
